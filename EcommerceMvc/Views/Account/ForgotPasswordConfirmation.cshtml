@{
    ViewData["Title"] = "Solicitud enviada";
}

<div class="container mt-5">
    <div class="alert alert-info">
        Si el email existe en el sistema, se enviará un enlace para recuperar la contraseña.
    </div>

    <a asp-action="Login" asp-controller="Account" class="btn btn-primary">
        Volver al login
    </a>
</div>